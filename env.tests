AUTH_ORIGIN=https://ingress.platform.gcp.oreilly.review
LOG_LEVEL=debug
# Needed so that local node apps do not reject self-signed certs.
NODE_TLS_REJECT_UNAUTHORIZED=0

###### Environment Specifiers
# This represents the environment we are running in.
ENV=development
# This represents the type of build we are using.
NODE_ENV=testing

###### Test specific variables
ORM_JWT_ALLOW_EXPIRED=true

###### App-specific environment variables
